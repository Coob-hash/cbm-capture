<?xml version="1.0" encoding="utf-8"?>
<!-- Where the camera writes the report's AFTER photo: this app's own cache, nowhere else. -->
<paths>
    <cache-path name="report-photos" path="report-photos/" />
</paths>
